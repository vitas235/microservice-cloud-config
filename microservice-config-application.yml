#注意：如果在记事本上编写，下面得缩进不要使用TAB来缩进，不然报错
spring:
  profiles:
    active:dev #激活开发环境配置

---
server:
  port:4001
spring:
  profiles: dev #开发环境
  application: 
    name: microservice-config-dev
        
---
server:
  port:4002
spring:
  profiles: prod #生产环境
  application:
    name: microservice-config-prod
        
#保存时注意，选择UTF-8保存
